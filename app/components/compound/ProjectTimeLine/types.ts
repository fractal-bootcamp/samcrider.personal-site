export type Project = {
  timelineTitle: string;
  projectTitle: string;
  projectDescription: string;
  projectLink: string;
  projectImage: string;
};

export type Fields = {
  projects: Project[];
};
